import React from "react";
// papago define used when a new card is created (attempt to get definition =meaning+examples)

const AddFlashcard = () => {
  return <div>AddFlashcard</div>;
};

export default AddFlashcard;
